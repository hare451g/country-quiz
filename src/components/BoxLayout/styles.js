import { css } from '@emotion/core';

const layoutContainerStyle = css`
  position: relative;
  z-index: 1;
`;

const titleStyle = css`
  color: white;
  padding: 8px 16px;
  margin: 0;
  font-size: 36px;
  font-weight: 700;
  text-transform: uppercase;
`;

export { layoutContainerStyle, titleStyle };
