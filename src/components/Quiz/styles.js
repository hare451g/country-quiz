import { css } from '@emotion/core';

const choiceContainerStyle = css`
  margin-bottom: 18px;
`;

const questionStyle = css`
  font-weight: bold;
  font-size: 24px;
  line-height: 36px;
  color: #2f527b;
`;

export { choiceContainerStyle, questionStyle };
